import { CleanStationNamePipe } from './clean-station-name.pipe';

describe('CleanStationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanStationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
